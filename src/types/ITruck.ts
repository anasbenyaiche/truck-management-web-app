export interface Truck {
  id: string;
  licensePlate: string;
  model: string;
  brand: string;
  year: number;
}

export interface TruckFormInputs {
  licensePlate: string;
  model: string;
  brand: string;
  year: number;
}
